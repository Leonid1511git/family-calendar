# Копируйте в .env.calendar и подставьте свои значения.
# .env.calendar не коммитится — используется для «expo start» и локальной разработки.
# Для APK токен задаётся в EAS (Environment variables).
# Имя .env.calendar выбрано, чтобы не путать с env-файлами соседних проектов (например .env или .env.finance).

EXPO_PUBLIC_TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
# EXPO_PUBLIC_TELEGRAM_BOT_NAME=FamilyCalendarApp_bot
# EXPO_PUBLIC_TELEGRAM_AUTH_PROXY_URL=https://family-calendar-22abd.web.app
