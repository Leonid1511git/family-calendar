# 🚀 ИНСТРУКЦИЯ ПО УСТАНОВКЕ И ЗАПУСКУ

## ✅ ВСЕ ИСПРАВЛЕНИЯ ВЫПОЛНЕНЫ

Проект полностью подготовлен для работы с Expo SDK 54 и Expo Go.

## 📋 Выполненные исправления

1. ✅ **Версии исправлены:**
   - React: `18.3.1` (совместимо с Expo SDK 54)
   - React Native: `0.76.5` (совместимо с Expo SDK 54)

2. ✅ **Удалены проблемные пакеты:**
   - `expo-build-properties` (не нужен для Expo Go)
   - `react-native-calendars` (требует нативной сборки)
   - `react-native-reanimated` (требует нативной сборки)
   - `react-native-vector-icons` (не используется)

3. ✅ **Упрощена конфигурация:**
   - Убраны ссылки на отсутствующие assets
   - Убрано логирование из кода
   - Упрощен `babel.config.js`

4. ✅ **Код адаптирован:**
   - CalendarScreen использует заглушку вместо Calendar
   - CreateEventScreen не использует DateTimePicker

## 🔧 УСТАНОВКА (выполните команды по порядку)

```bash
# 1. Полная очистка
rm -rf node_modules
rm -rf package-lock.json
rm -rf .expo
npm cache clean --force

# 2. Установите expo сначала
npm install expo@~54.0.0

# 3. Установите правильные версии React через expo install
npx expo install react react-native

# 4. Установите все остальные зависимости
npm install

# 5. Запустите с очисткой кэша
npx expo start --clear
```

## ⚠️ КРИТИЧЕСКИ ВАЖНО

**НЕ МЕНЯЙТЕ ВЕРСИИ React и React Native ВРУЧНУЮ!**

Версии в `package.json` уже правильные:
- `"react": "18.3.1"` ✅
- `"react-native": "0.76.5"` ✅

Если вы случайно измените их на React 19 или React Native 0.81, проект **НЕ ЗАПУСТИТСЯ**.

## 📱 После запуска

1. Откройте Expo Go на телефоне
2. Отсканируйте QR-код из терминала
3. Приложение должно загрузиться без ошибок

## 🔍 Если что-то не работает

1. Убедитесь, что версии в `package.json` правильные
2. Выполните полную очистку (команды выше)
3. Используйте `npx expo start --tunnel` если есть проблемы с сетью

## 📝 Примечания

- Календарь временно отключен (используйте режимы "Неделя" или "День")
- DateTimePicker временно отключен (можно добавить позже через development build)
- Все остальные функции должны работать

